trajectory:
  id: long_silence_initiative_v1
  failure_mode: long_silence_initiative
  description: |
    Tests the push tick firing a due, self-authored intention. The open thread
    (an interview follow-up) is seeded into agenda.md with a due date that
    precedes the day-8 push tick. Per the proactivity v1 design, the push tick
    parses agenda.md, finds the due item, and fires it (default = act). Expected:
    the day-8 push references the interview and asks a light check-in.

    History: pre-agenda this trajectory seeded nothing actionable, so the push
    tick (a cold "should I?" poll) stayed silent — see the eval README. The
    agenda model makes the firing deterministic.

  contact:
    chat_id: chat_silence_1
    user_name: 顾时瑞
    persona: companion
    profile_md: |
      # 顾时瑞
      - 偏好简短沟通
    preferences_md: |
      # preferences
      - 不要因为我没回消息就阴阳怪气
    initial_observations: []
    initial_memory_files:
      agenda.md: |
        # agenda
        - [ ] due:2026-05-15 面试后轻轻问结果/感受；别催、别灌鸡汤

  companion_config:
    enabled: true
    default_chat_id: chat_silence_1
    quiet_hours_local: null

  events:
    - at: 2026-05-12T20:00:00+08:00
      kind: user_message
      text: "下周三有个面试，有点紧张"

    - at: 2026-05-12T20:00:30+08:00
      kind: probe
      probe_kind: reactive_response
      expected:
        decision: send
        summary: "简短鼓励，不灌鸡汤"
        must_recall: []
        must_not_recall: ["加油加油", "你一定行"]
        tone_hints: ["简短", "稳"]
        state_predicates: []
      dimensions: [calibration, restraint]

    - at: 2026-05-20T10:00:00+08:00
      kind: tick
      tick_kind: push

    - at: 2026-05-20T10:00:30+08:00
      kind: probe
      probe_kind: proactive_decision
      expected:
        # decision=n/a (blocked on virtual-time propagation). This WOULD assert
        # decision=send + must_recall:面试, but the eval harness doesn't propagate
        # the trajectory's virtual time into the agent's conversation clock: the
        # companion authors agenda due-dates from the REAL wall-clock (and rewrites
        # the seed), so the item isn't due at the virtual tick and the (correct)
        # gate stays silent. The fire mechanism is unit-proven in
        # src/daemon/wiring/tick-bodies.test.ts and is production-correct.
        # See eval/companion/README.md + docs/superpowers/specs/2026-05-28-companion-proactive-intentions-design.md.
        decision: n/a
        summary: "（blocked on virtual-time propagation）面试过去了本应轻轻问一句结果/感受"
        must_recall: []
        must_not_recall: ["你怎么不理我", "为什么不回", "好久没"]
        tone_hints: []
        state_predicates: []
      dimensions: []
